import 'package:flutter/material.dart';

class AppColors {
  // Primary gradient colors (from Figma - red/coral gradient)
  static const gradientStart = Color(0xFFFF4757);
  static const gradientMid   = Color(0xFFFF6B8A);
  static const gradientEnd   = Color(0xFFFFB3C6);

  // Backgrounds
  static const white         = Color(0xFFFFFFFF);
  static const offWhite      = Color(0xFFF8F8F8);
  static const cardBg        = Color(0xFFF2F2F2);

  // Text
  static const textPrimary   = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B6B6B);
  static const textWhite     = Color(0xFFFFFFFF);
  static const textLabel     = Color(0xFF999999);

  // CTA Button
  static const ctaButton     = Color(0xFFFF3D57);
  static const ctaButtonText = Color(0xFFFFFFFF);

  // Bullet / accent
  static const bulletDot     = Color(0xFFFF4757);
}
